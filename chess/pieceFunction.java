package chess;

import java.util.ArrayList;
import java.lang.Math;

import chess.ReturnPiece.PieceFile;
import chess.ReturnPiece.PieceType;

public class pieceFunction {

// Use callPiece to call the legality function associated with the given pieceType
    public static boolean callPiece (PieceFile startFile, PieceFile endFile, int startRank, int endRank, ArrayList<ReturnPiece> piecesOnBoard, PieceType type) {
        // IN PROGRESS
        return false;
    }
}
